npm init -y
npm install

docker run -d -p 27017:27017 --name mongodb mongo
docker run -d -p 5672:5672 -p 15672:15672 --name rabbitmq rabbitmq:management

nodemon start app.js

copy the index.html file path in browser (Change the userId you will get userId in login response)

Register a user using username, email and password

Login the user using email and password

Use the token sent in login response api for createNotification api

All the created Notification  will be visible on index.html 
